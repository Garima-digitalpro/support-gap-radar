# Export analytics data

Admins can export analytics tables as CSV from **Analytics > Export**.

Each CSV export contains a maximum of 10,000 rows on every plan. Narrow the date range or apply filters when the result exceeds the limit. Exports are prepared asynchronously and remain available for seven days.

Audit-event exports use the same 10,000-row maximum.
