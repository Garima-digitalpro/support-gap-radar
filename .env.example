# Server-side only. Never expose this variable through Vite or commit a real key.
OPENAI_API_KEY=

# GPT-5.6 Sol alias required by the hackathon brief.
OPENAI_MODEL=gpt-5.6

# Low-cost embedding model used for semantic clustering and retrieval.
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
